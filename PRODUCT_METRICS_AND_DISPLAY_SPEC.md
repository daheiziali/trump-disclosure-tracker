# 特朗普交易披露追踪 - 展示与统计口径说明

## 1. 文档目的

本文档用于明确「特朗普交易披露追踪」用户端页面的展示结构、排行榜统计范围、投资主题占比口径、资产状态判断逻辑，以及「查看更多」进入完整列表后的默认筛选和排序规则。

本文档面向产品、设计、前端、后端和数据处理人员，是当前版本的重要验收依据。

## 2. 产品总体结构

产品用户端分为两个核心维度：

1. 交易排行榜
2. 最新交易披露

其中「交易排行榜」下包含四个模块：

1. 持仓规模
2. 高频交易
3. 买入规模
4. 卖出规模

「最新交易披露」用于展示最近披露或最近发生的交易记录，帮助用户快速查看最新买入、卖出动态。

## 3. 数据范围

### 3.1 数据来源

当前阶段仅使用美国政府道德办公室 OGE 官方披露数据。

OGE 页面：

```text
https://www.oge.gov/web/OGE.nsf/Officials%20Individual%20Disclosures%20Search%20Collection?OpenForm
```

检索方式：

```text
在 name 字段输入 trump 进行筛选
```

### 3.2 展示资产范围

用户端仅展示投资用户关注的可交易证券：

- 个股
- ETF
- 基金

用户端不展示：

- 市政债
- 企业债
- 票据
- 现金及银行账户
- 房地产
- 私营公司权益
- 授权收入、版税、经营收入
- 加密资产、代币、NFT 或数字资产

注意：Coinbase、Strategy/MicroStrategy 等上市公司个股可以作为普通股票展示，但不得归类为加密资产。

## 4. 金额展示口径

OGE 披露中的资产价值和交易金额均为区间，不是精确金额。

系统统一采用区间中点进行估算：

```text
区间中点 = (区间下限 + 区间上限) / 2
```

用户端主展示字段统一为：

```text
披露规模估算
```

不在主列表中并列展示「基准区间估算」「买入区间估算」「卖出区间估算」。这些字段可保留在资产详情、后台复核或说明弹窗中。

金额单位建议：

- 表头写明单位：美元
- 大额展示可用「万美元」
- 列表内统一格式，例如：`5042.05 万美元`

说明文案：

```text
披露规模估算基于 OGE 年度披露区间与后续交易披露区间，按区间中点估算，不代表真实账户金额、持股数量或实时市值。
```

## 5. 交易排行榜模块

### 5.1 持仓规模

统计目标：

展示特朗普披露口径下规模最大的可识别证券资产。

统计范围：

```text
覆盖全部可识别披露记录，包含年度基准资产与 2026 年至今交易增量。
```

排序规则：

```text
按披露规模估算降序排列。
```

计算逻辑：

```text
披露规模估算 = 年报基准区间中点合计 + 2026 年至今买入区间中点合计 - 2026 年至今卖出区间中点合计
```

展示重点：

- 资产名称
- 代码
- 投资主题
- 披露规模估算
- 当前状态

### 5.2 高频交易

统计目标：

展示 2026 年至今反复出现在交易披露中的重点资产。

统计范围：

```text
仅统计 2026 年至今 OGE 已披露的买入、卖出交易记录。
```

统计口径：

```text
同一资产按标准化名称或证券代码合并。
买入 1 次、卖出 1 次均计为 1 条披露交易记录。
```

进入高频交易榜的建议门槛：

```text
交易次数不少于 3 次。
```

排序规则：

```text
按交易次数降序排列；交易次数相同，则按该资产 2026 年至今披露交易金额区间中点合计降序排列。
```

前端推荐文案：

```text
2026 年至今反复买卖 3 次及以上的重点资产。
```

图表中的总数应表述为：

```text
披露交易记录
```

避免直接写成「真实交易次数」或「实际下单次数」。

### 5.3 买入规模

统计目标：

展示 2026 年至今买入规模较大的资产。

统计范围：

```text
仅统计 2026 年至今 OGE 已披露的买入交易。
```

计算逻辑：

```text
买入规模 = 2026 年至今买入交易金额区间中点合计
```

排序规则：

```text
按买入规模降序排列。
```

### 5.4 卖出规模

统计目标：

展示 2026 年至今卖出规模较大的资产。

统计范围：

```text
仅统计 2026 年至今 OGE 已披露的卖出交易。
```

计算逻辑：

```text
卖出规模 = 2026 年至今卖出交易金额区间中点合计
```

排序规则：

```text
按卖出规模降序排列。
```

## 6. 投资主题占比口径

每个排行榜模块下的投资主题占比，均基于该模块自身的数据范围、时间范围和统计口径单独计算。

统一说明：

```text
各排行榜下的投资主题占比，均按当前模块的数据范围单独计算，不代表全组合统一占比。
```

### 6.1 持仓规模主题占比

统计范围：

```text
持仓规模模块内的可识别资产。
```

统计口径：

```text
按披露规模估算汇总，再按投资主题分组。
```

用户理解：

反映当前披露口径下的资产结构。

### 6.2 高频交易主题占比

统计范围：

```text
2026 年至今进入高频交易统计池的资产。
```

统计口径：

```text
按交易记录条数汇总，再按投资主题分组。
```

注意：

高频交易主题占比必须按次数统计，不按金额统计。

用户理解：

反映 2026 年至今被反复买卖的主题方向。

### 6.3 买入规模主题占比

统计范围：

```text
2026 年至今披露买入交易。
```

统计口径：

```text
按买入金额区间中点合计，再按投资主题分组。
```

用户理解：

反映新增资金更集中的方向。

### 6.4 卖出规模主题占比

统计范围：

```text
2026 年至今披露卖出交易。
```

统计口径：

```text
按卖出金额区间中点合计，再按投资主题分组。
```

用户理解：

反映减持或退出更集中的方向。

## 7. 「查看更多」进入完整列表的逻辑

四个排行榜模块进入「查看更多」后，应进入统一的排行榜详情页或完整列表页，但需要根据来源模块自动切换默认视图。

建议路由参数：

```text
/rankings?tab=holding
/rankings?tab=frequency
/rankings?tab=buy
/rankings?tab=sell
```

默认规则：

| 来源模块 | 默认榜单 | 默认排序 | 默认统计范围 |
|---|---|---|---|
| 持仓规模 | 持仓规模 | 披露规模估算降序 | 全部披露口径 |
| 高频交易 | 高频交易 | 交易次数降序 | 2026 年至今 |
| 买入规模 | 买入规模 | 买入规模降序 | 2026 年至今 |
| 卖出规模 | 卖出规模 | 卖出规模降序 | 2026 年至今 |

详情页顶部必须明确展示当前口径，例如：

```text
当前榜单：卖出规模
统计范围：2026 年至今
```

或：

```text
当前榜单：持仓规模
统计范围：全部披露口径
```

MVP 阶段建议不开放自由时间周期切换，只展示清晰口径。后续可扩展：

- 近 30 日
- 近 90 日
- 今年以来
- 自定义时间

## 8. 最新交易披露

统计目标：

展示最新披露或最新发生的买入、卖出记录。

统计范围：

```text
2026 年至今 OGE 已披露交易记录。
```

首页默认展示：

```text
前 10 条最新交易。
```

默认排序：

```text
交易日降序；交易日相同，则按披露日降序。
```

字段建议：

- 资产名称
- 代码
- 投资主题
- 动作：买入/卖出
- 交易日
- 金额区间
- 披露日

## 9. 资产状态体系

### 9.1 前端状态筛选项

最终状态筛选项：

```text
全部
新进
增持
减持
疑似清仓
疑似新进
仍持有
```

其中「全部」只是筛选入口，不是资产状态。

### 9.2 状态判断变量

状态判断需要以下字段：

```text
baseline_midpoint       年报基准区间中点合计
buy_count               2026 年至今买入次数
sell_count              2026 年至今卖出次数
purchase_midpoint       2026 年至今买入区间中点合计
sale_midpoint           2026 年至今卖出区间中点合计
last_action             最近一次 2026 年至今交易动作
last_trade_date          最近一次交易日
trade_chain              该资产按交易日排序后的完整交易链
```

### 9.3 状态判断顺序

状态判断必须按顺序执行：

```text
1. 疑似新进
2. 疑似清仓
3. 新进
4. 增持
5. 减持
6. 仍持有
```

顺序很重要。例如某资产最近一次是买入，但此前曾触发疑似清仓，则应优先显示「疑似新进」，而不是普通「增持」。

### 9.4 疑似清仓

用户理解：

```text
最近在卖，且卖出规模已经接近或覆盖此前可识别持仓基础。
```

判断逻辑：

```text
last_action = 卖出
且 sale_midpoint >= 80% * 可识别持仓基础
```

可识别持仓基础建议定义为：

```text
可识别持仓基础 = 年报基准区间中点 + 疑似清仓判断前的累计买入区间中点
```

如果技术实现暂时无法区分「疑似清仓判断前」的累计买入，可在 MVP 阶段使用：

```text
可识别持仓基础 = 年报基准区间中点 + 2026 年至今累计买入区间中点
```

阈值建议：

```text
80%
```

原因：

OGE 只披露金额区间，不披露股数和成交价格。使用 80% 作为疑似阈值，比 100% 更适合区间估算和价格波动场景。

### 9.5 疑似新进

用户理解：

```text
此前可能已经基本退出，后续最近一次又出现买入，可能重新建仓。
```

判断逻辑：

```text
此前曾出现疑似清仓信号
且疑似清仓之后又出现至少 1 次买入
且最近一次交易为买入
```

注意：

「疑似新进」不是简单的「年报基准没有该资产，但 2026 年后有买入也有卖出」。这种旧定义过宽，容易误判。

### 9.6 新进

用户理解：

```text
第一次进入披露交易视野。
```

判断逻辑：

```text
baseline_midpoint = 0
buy_count = 1
sell_count = 0
last_action = 买入
```

注意：

如果年报基准没有该资产，但 2026 年至今出现大于 1 次买入，应显示「增持」，不是「新进」。

### 9.7 增持

用户理解：

```text
最近一次披露交易为买入。
```

判断逻辑：

```text
last_action = 买入
且不满足「疑似新进」
且不满足「新进」
```

适用情况：

- 年报基准已有该资产，最近一次交易为买入。
- 年报基准没有该资产，但 2026 年至今买入次数大于 1，最近一次交易为买入。
- 该资产 2026 年至今有买有卖，但最近一次交易为买入，且此前没有触发疑似清仓。

### 9.8 减持

用户理解：

```text
最近一次披露交易为卖出。
```

判断逻辑：

```text
last_action = 卖出
且不满足「疑似清仓」
```

注意：

即使年报基准没有该资产，只要 2026 年至今出现过买入，最近一次为卖出，且不满足疑似清仓，也归为「减持」。用户端不再显示「状态不明」。

### 9.9 仍持有

用户理解：

```text
年报基准中已有该资产，但 2026 年后没有披露买入或卖出。
```

判断逻辑：

```text
baseline_midpoint > 0
buy_count = 0
sell_count = 0
```

## 10. 状态展示文案

前端说明建议：

```text
新进：年报未见该资产，2026 年后首次披露买入，且尚无卖出记录。
增持：最近一次披露交易为买入。
减持：最近一次披露交易为卖出，且未触发疑似清仓。
疑似清仓：最近一次披露交易为卖出，且累计卖出规模接近或覆盖此前可识别持仓基础。
疑似新进：此前曾出现疑似清仓信号，之后最近一次披露交易重新出现买入。
仍持有：年报基准已有该资产，2026 年后暂无买入或卖出披露。
```

## 11. 资产交易详情

资产详情页或详情模块应展示单一资产的完整交易披露链。

默认行为：

- 默认选择排行榜中披露规模估算最高的资产。
- 用户点击其他资产卡片后，切换为该资产详情。
- 不应因为点击切换导致其他资产卡片消失。

详情展示建议：

- 资产名称
- 代码
- 投资主题
- 当前状态
- 披露规模估算
- 最近交易
- 匹配披露条数
- 交易披露链

交易披露链排序：

```text
默认按交易日降序，其次按披露日降序。
```

交易披露链字段：

- 来源：年报交易明细 / 2026 278-T 披露
- 资产名称
- 动作：买入/卖出
- 交易日
- 金额区间
- 披露日
- 投资主题

## 12. 名称与代码标准化

不同来源可能对同一资产有不同写法，例如：

```text
Alphabet Inc Class A
ALPHABET INC CL-A
ALPHABET INC CLASS A
Alphabet Inc Cl A
```

用户端必须尽量统一为标准资产名称和标准 ticker。

标准化原则：

- 优先使用 ticker 合并同一资产。
- 无 ticker 时使用标准化资产名称合并。
- ETF 需要使用 ETF 清单映射，避免把发行商或管理人股票代码误识别为 ETF 代码。
- 股票、ETF、基金需要分别维护映射规则。
- 后台复核允许补充或修正 ticker。

## 13. 合规与限制说明

产品应保留低优先级说明，但不应在首页核心位置过度突出。

必须保留的合规口径：

```text
本产品基于 OGE 公开披露文件整理。披露金额为官方区间，页面估算为区间中点汇总，不代表真实账户金额、持股数量、成交价格或实时市值。本产品不构成投资建议。
```

不建议在付费介绍页过度强调「非实时信号」，但在正式产品的数据说明或底部注释中需要保留。

## 14. 验收要点

前端验收：

- 首页整体分为「交易排行榜」和「最新交易披露」两大维度。
- 交易排行榜包含持仓规模、高频交易、买入规模、卖出规模四个模块。
- 每个模块「查看更多」进入后，默认排序和统计范围与来源模块一致。
- 持仓规模使用全部披露口径；高频交易、买入规模、卖出规模使用 2026 年至今口径。
- 高频交易按交易记录条数统计，不按金额统计。
- 每个排行榜模块下的投资主题占比按当前模块独立统计。
- 用户端状态筛选包含：全部、新进、增持、减持、疑似清仓、疑似新进、仍持有。
- 用户端不出现「状态不明」。
- 主列表金额字段统一为「披露规模估算」。
- 加密资产、代币、NFT、数字资产不得在用户端展示。

后端验收：

- 可按资产/ticker 合并交易链。
- 可区分年度基准资产和 2026 年至今增量交易。
- 可按模块输出独立排行榜数据。
- 可按模块输出独立投资主题占比。
- 可按最终状态规则输出资产状态。
- 保留基准、买入、卖出拆项数据，但用户端主列表不默认展示。

